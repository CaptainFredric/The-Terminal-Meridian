flask>=3.1,<4.0
flask-cors>=4.0
# Production WSGI server (used by Procfile / render.yaml).
gunicorn>=21.2

# Optional — only needed if you set STRIPE_SECRET_KEY to enable billing.
stripe>=8.0

# Optional — AI commentary providers (lazy-imported in backend/ai_commentary.py).
# Uncomment whichever you intend to use.
# openai>=1.40
# anthropic>=0.34
